name: 'hcltm'
description: 'An action to run hcltm commands in your GitHub Workflows'
author: 'Christian @xntrik Frichot'
inputs:
  command:
    description: 'What hcltm command to run'
    required: true
    default: 'validate'
  files:
    description: 'Target files'
    required: true
    default: '*'
runs:
  using: 'docker'
  image: 'docker://xntrik/hcltm-action:latest'
  args:
    - ${{ inputs.command }}
    - ${{ inputs.files }}
